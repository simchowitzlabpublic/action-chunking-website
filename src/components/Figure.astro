---
interface Props {
  src: string;
  alt?: string;
  width?: string;
  height?: string;
}

const { src, alt, width, height } = Astro.props;
---

<img src={src} alt={alt} style={`width: ${width}; height: ${height ? height : '15rem'};`}/>
